# Optional local hooks: `uvx pre-commit install` once per clone.
# CI runs the same checks (lint job), so skipping this is never fatal.
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.14.14
    hooks:
      - id: ruff-check
